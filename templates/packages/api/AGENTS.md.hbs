# AGENTS.md (api package)

This package inherits all rules from the repository root `AGENTS.md`.

## Package Scope

API services, endpoints, and server-side integrations.

## Commands

```bash
{{packageManager}} --filter api run test
{{packageManager}} --filter api run build
```

## Notes

- Validate inputs and enforce auth boundaries.
- Maintain API contracts and update tests alongside changes.
