# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code). **AGENTS.md is the canonical source of truth** for all agent rules; this file only adds Claude-specific hints and command shortcuts.

## Quick Start

```bash
{{packageManager}} install
{{packageManager}} run lint
{{packageManager}} run format:check
{{packageManager}} test
```

## Claude-Specific Workflow

1. Read `AGENTS.md` first (scope + safety).
2. Use prompts from `/prompts/` for consistent task framing.
3. Finish with a clean validation pass.

## Development Commands

### Setup & Dependencies
```bash
{{#if packageManager}}
{{packageManager}} install
{{else}}
npm install
{{/if}}
chmod +x scripts/agent-guardrails.sh
```

### Quality & Validation
```bash
{{#if packageManager}}
{{packageManager}} run guardrails
{{packageManager}} run lint
{{packageManager}} run format:check
{{#if useTypeScript}}
{{packageManager}} run type-check
{{/if}}
{{packageManager}} run test
{{else}}
npm run guardrails
npm run lint
npm run format:check
{{#if useTypeScript}}
npm run type-check
{{/if}}
npm run test
{{/if}}
```

### Build & Development
```bash
{{#if packageManager}}
{{packageManager}} run build
{{packageManager}} run dev
{{else}}
npm run build
npm run dev
{{/if}}
```

{{#if useMonorepo}}
### Workspace Management
```bash
{{packageManager}} --filter {{packages.[0]}} run build
{{packageManager}} --filter {{packages.[0]}} run test
```
{{/if}}

## Architecture Pointers

- `AGENTS.md` – canonical AI collaboration rules
- `docs/` – documentation index and guides
- `scripts/` – guardrails and automation

---

**Project**: {{projectName}}
**Author**: {{author}}
**Last Updated**: {{currentYear}}
